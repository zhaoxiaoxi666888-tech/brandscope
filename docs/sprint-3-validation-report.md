# Sprint 3 Validation Report

报告日期：2026-07-21

## 1. 验收结论

**本轮未通过真实 AI 质量验收，不建议进入部署阶段。**

原因不是模型输出已被判定不合格，而是当前本地环境没有配置 OpenAI 与 Brave Search 密钥，无法运行三个真实案例。按照验收规则，不使用 Mock 或演示数据冒充真实结果，也不在缺少证据的情况下声称 Prompt 已改善。

## 2. 真实环境检查

| 检查项 | 结果 |
|---|---|
| `.env` | 存在，未纳入 Git |
| 当前 AI Provider | Mock |
| 当前 Search Provider | 默认 Mock |
| OpenAI 服务端密钥 | 未配置 |
| Brave Search 服务端密钥 | 未配置 |
| 浏览器与仓库密钥暴露 | 未发现 |
| Mock 无密钥运行 | 通过 |
| 自动化测试调用付费 API | 否 |
| 真实模式缺钥行为 | 已由“静默回退”修复为明确中文错误 |
| 缺钥请求的数据保护 | 实测研究记录请求前后均为 6 条，旧成果未覆盖 |

报告及日志仅记录密钥状态，不记录密钥值。

## 3. 三个固定案例运行记录

| 案例 | Provider | 来源 | Research | Insights | Brief | 耗时 | Token | 成本 | 结论 |
|---|---|---:|---:|---:|---:|---:|---:|---:|---|
| 安克创新｜德国消费电子市场 | 未运行 | — | — | — | — | — | — | — | 缺少真实密钥 |
| Dyson｜中国高端个护市场 | 未运行 | — | — | — | — | — | — | — | 缺少真实密钥 |
| Apple｜中国品牌市场研究 | 未运行 | — | — | — | — | — | — | — | 缺少真实密钥 |

开始时间、完成时间、不同域名、重试次数、错误和成本均不能在未发起真实调用时伪造。当前 Provider 也尚未将 token usage 与重试次数输出到独立验收记录，这是下一次真实验收前必须补齐的 P1。

## 4. 来源质量审查

未生成真实来源，因此没有执行逐 URL 打开、标题匹配、发布方、发布时间、相关性和 A–D 分级。现有演示来源全部属于演示内容，不纳入真实来源质量评分。

需要人工核验的检查表：链接可访问；标题与正文一致；发布方身份可信；日期可在页面确认；内容与研究目标直接相关；没有重复 URL；单域占比不超过合理范围；搜索摘要不被当作全文事实；D 级来源不支撑确定性结论。

## 5. Research 六模块评分

未运行真实案例，六模块的相关性、具体性、来源支撑、事实与推断区分、中文自然度、空泛程度和决策价值均不评分。用 Mock 文案评分会错误抬高真实能力判断。

## 6. 洞察审查

未生成真实洞察，因此没有做“保留 / 修改 / 删除”判定。正式验收时，每条必须包含观察、判断、行动影响、依据和推断边界；“用户重视品质”“市场竞争激烈”等泛化表述默认删除。

## 7. Brand Brief 评价

未生成真实 Brief，九章节不评分。当前只能确认代码层面只把 `CONFIRMED` 洞察交给 Brief Service，不能据此推断真实简报质量。

结论：**不能用于真实工作**。原因是本轮没有真实输出可供核验，而不是对演示简报的评价。

## 8. Prompt 版本与前后对比

| Prompt | 当前版本 | 本轮变化 | 原因 |
|---|---:|---|---|
| Research | 2.0.0 | 未修改 | 没有真实失败样本，避免盲调 |
| Insight | 2.0.0 | 未修改 | 没有真实失败样本，避免品牌硬编码 |
| Brief | 2.0.0 | 未修改 | 没有真实失败样本，避免虚假改善结论 |

没有完成“初始运行 → 调整 → 关键模块复跑”，因此没有前后对比，也不声称优化成功。

## 9. P0 修复

- 修复 `AI_PROVIDER=openai` 且缺少 `OPENAI_API_KEY` 时静默使用 Mock 的问题。
- 修复 `SEARCH_PROVIDER=web` 且缺少 `BRAVE_SEARCH_API_KEY` 时静默使用 Mock 的问题。
- Provider 改为调用时解析配置，缺钥错误可由 API 捕获并返回自然中文，不阻断页面加载。
- 增加回归测试，保证真实模式缺钥明确失败、Mock 模式始终无密钥可运行。

## 10. P1 / P2 清单

### P1

- OpenAI usage、实际重试次数、单阶段耗时尚未形成可导出的验收记录。
- 成本估算尚未绑定模型版本与当日官方价格，不能可靠计算。
- Brave 结果目前使用搜索摘要，尚未对正文与摘要一致性做人工核验。
- 来源类型与官方站识别采用启发式规则，可能误分类。
- 服务器端尚无跨请求幂等锁；客户端 `busy` 可避免普通重复点击，但不能完全防御并发重复付费请求。

### P2

- 需要维护真实来源人工评分表和案例复跑历史。
- 可以在不改变产品流程的前提下增加 Provider 级可观测性与成本预算报警。

## 11. 验收标准逐项判断

| 标准 | 结果 |
|---|---|
| 三个案例至少两个完成真实研究 | 未通过 |
| 关键来源可打开且相关 | 未验证 |
| 事实与 AI 推断清晰区分 | 结构已支持，真实输出未验证 |
| 无明显虚构精确数据 | 未验证 |
| 半数洞察有决策价值 | 未验证 |
| Brief 仅使用已确认洞察 | 代码与自动化测试通过 |
| 至少一份 Brief 修改后可讨论 | 未通过 |
| 成本、耗时和失败可追踪 | 未通过 |
| Mock 完整运行 | 通过 |
| lint、typecheck、test、build | 全部通过；13/13 测试通过 |

## 12. 是否建议部署

**不建议。** 在真实密钥、固定案例输出、来源人工审查、token/成本记录和 Prompt 至少一轮有证据的迭代完成前，部署会把工程可运行误当成产品质量已验证。

## 13. 下一轮唯一建议

由项目所有者在本地 `.env` 配置 OpenAI 与 Brave Search 密钥，然后只重跑本报告中的三个固定案例。不得增加功能；先完成来源审查、模块评分和一次有证据的 Prompt 迭代，再决定是否部署。
